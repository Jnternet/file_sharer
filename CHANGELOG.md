# 更新日志

## 未发布

- 手机访问时隐藏「登记文件夹」入口（移动浏览器普遍不支持目录选择），保留「登记文件」并提示改用桌面端。
- Release 工作流不再覆盖 Release 说明（改为覆盖同名产物），说明仍由本文件维护。

## 0.1.0 — 2026-09-18

首个发布版。核心模型：**网页是记录区，只登记文件位置；别人点击下载才开始传输；服务器只做定向转发，不记录不留存。**

### 功能

- **单产物 + 网页访问**：前端 HTML/CSS/JS 编译期内嵌，`cargo build --release` 得到的可执行文件即完整服务；
  启动后打印本机与局域网地址。
- **记录区**：选择文件/文件夹只登记"位置"（名称、大小、相对路径、内容 SHA-256），不发送任何文件字节；
  记录的条目按需拉取（不广播），对端离线即消失。
- **点击才传输**：只有下载方点「下载」后，分享者才开始读文件并按 1 MiB 分块发送；服务器按连接绑定逐帧定向转发。
- **不无操作直接下载**：接收完成后只出现保存入口（单文件「保存」/文件夹「打包下载 .zip」），不点不落盘。
- **断点续传**：内容寻址 `shareId` + 接收端 IndexedDB 分块 + `resume-state` 协商偏移，重连只补传缺失分块。
- **自动校验**：登记时预哈希；发送中复算（检出源文件被修改）；接收端落盘后复算比对，失败自动重传。
- **单文件/文件夹自动区分**：单文件直存；多文件或带目录结构按文件夹传输，保留相对路径并可打包 ZIP（带打包进度）。
- **空桶服务器**：内存里只有在线会话与转发绑定；不落盘、不记录文件清单（由架构测试强制）。

### 工程

- Rust 64 例测试（单元/集成/架构，含 18 例 WebSocket 转发集成）+ 前端 89 例（协议回环、SHA-256、分帧、ZIP 等）。
- 真实浏览器端到端脚本（无头 Firefox + WebDriver BiDi）：登记 → 拉取 → 未点击前零传输 → 点击下载 → 校验 →
  显式保存逐字节核对 → 文件夹 ZIP（python3 解压校验）→ 预置断点续传。
- 发布产物：Linux（静态 musl 与 glibc）与 Windows x86_64 可执行文件。

### 已知限制

- 无鉴权，仅适用于可信局域网；分享者必须在线。
- 同一分享者同时只跑一条下载流（服务器二进制转发按连接绑定）。
- 文件夹 ZIP 上限 4 GiB（ZIP32）。
